%% sequenceDiagram
%%     participant Frontend as 🖥️ 前端界面
%%     participant DifyAPI as 🔗 Dify API 接口
%%     participant Workflow as 🔄 Workflow 引擎
%%     participant LLM as 🧠 大语言模型（LLM）

%%     Frontend->>+DifyAPI: 发送教学请求（Markdown 格式）
%%     DifyAPI->>Workflow: 解析教学场景内容
%%     Workflow->>LLM: 调用大模型生成结构化教案
%%     LLM-->>Frontend: 流式返回结果

sequenceDiagram
    User->>+前端: 发起教学操作
    前端->>+状态管理: 检查本地存储
    状态管理-->>-前端: 返回水合后状态
    前端->>+服务端: 同步最新状态    服务端-->>-前端: 返回增量更新
